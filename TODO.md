# TODO

- [ ] Verify why login inputs can’t be focused/typed (email/password).
- [ ] Add `name`, `id`, and `disabled={loading}` logic checks (if any) and ensure inputs are not covered by an overlay.
- [ ] Implement a safe fix in `src/routes/login.tsx` to prevent event blocking and ensure proper controlled inputs.
- [ ] Run typecheck/build to confirm no TS errors.

